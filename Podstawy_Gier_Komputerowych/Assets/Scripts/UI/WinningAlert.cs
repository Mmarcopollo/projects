using UnityEngine;

public class WinningAlert : MonoBehaviour
{
    /*
    public GameObject Mutations;

    // Update is called once per frame
    void Update()
    {
        if (Mutations.GetComponent<MutationChoice>().NumberOfMutations >= 6)
        {
            Time.timeScale = 0;
            this.GetComponent<Canvas>().enabled = true;
        }
    }
    */
}